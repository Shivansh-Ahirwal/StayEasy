# Leave empty when using Vite dev proxy to Django (see vite.config.js).
# Production (Vercel): full API origin without trailing slash, e.g.:
# VITE_API_URL=https://your-api.onrender.com

VITE_API_URL=
